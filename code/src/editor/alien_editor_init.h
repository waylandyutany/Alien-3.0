#pragma once

#if !defined(AE_EDITOR_INIT_H) && defined(AE_EDITOR)
#define AE_EDITOR_INIT_H

namespace ae { namespace editor {

	struct S_Init
	{
	};
} } //namespace ae { namespace editor {

#endif // #if !defined(AE_EDITOR_INIT_H) && defined(AE_EDITOR)
